sonar.projectKey=mitchins_clipdisk
sonar.organization=mitchins
sonar.projectName=clipdisk

sonar.sources=Sources
sonar.tests=Tests
sonar.sourceEncoding=UTF-8

# Coverage — generic XML converted from lcov on the macOS test runner
# File is only present on main-push runs; sonar gracefully skips if absent
sonar.coverageReportPaths=coverage.xml
